1. node는 pool 방식으로 하는게 제일 편함
2. \0은 string terminator로 기능함
3. for(node child : parent->children) 을 사용하면
pythond에서 (for child in children)과 같이 편하게 사용할 수 있음
4. recursive한 함수를 작성할 때 return값이 있는지 없는지 잘 생각하기
